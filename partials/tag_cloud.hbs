
<div class="card">
    <div class="card-title">
        <h4>标签云</h4>
    </div>
    <div class="card-content">
        {{#foreach tags}}
            <a href="{{url}}" class="label label-primary" target="_blank">{{name}}</a>
        {{/foreach}}
    </div>
</div>